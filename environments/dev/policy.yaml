---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: environment-quota
  namespace: platform-dev
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 2Gi
    limits.cpu: "4"
    limits.memory: 4Gi
