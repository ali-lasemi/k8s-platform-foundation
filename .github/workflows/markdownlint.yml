name: markdownlint

on:
  pull_request:
  push:
    branches: [main]

jobs:
  markdown:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - uses: DavidAnson/markdownlint-cli2-action@v24
